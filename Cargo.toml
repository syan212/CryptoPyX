[package]
name = "_cryptopyx"
# Update pyproject version too
version = "0.1.2"
description = "Rust backend for the cryptopyx Python package"
edition = "2024"
rust-version = "1.85.0"

[lib]
name = "_cryptopyx"
crate-type = ["cdylib"]

[dependencies]

[dependencies.pyo3]
version = "0.27.0"
features = ["abi3-py310"]
